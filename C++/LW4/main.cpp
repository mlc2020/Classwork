#include <iostream>
using namespace std;

bool isPrime(int n) {
    int divisor{};
    int counter{};

    for(divisor= 1; divisor <= n; divisor++) {
        if(n % divisor == 0) {
            counter++;
        }
    }
    if (counter == 2) {
        pr
    }
    return prime;
}

int main() {

    return 0;
}